const express = require('express');
const router = express.Router();
const authentication = require('../utils/authentication');

const keywordsAreaController = require('../controller/keywordController');

module.exports = router;